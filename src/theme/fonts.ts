// Font family constants — loaded via expo-font in _layout.tsx
export const Fonts = {
  display: 'BungeeShade',     // Titles, logo — chunky game feel
  heading: 'Bungee',          // Section headers, buttons
  body: 'Nunito',             // Body text, stats, labels
  bodyBold: 'Nunito-Bold',
  mono: 'tabular-nums',       // Timer, scores (system feature)
} as const;
